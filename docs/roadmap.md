# Product Roadmap

This public repository is a showcase (docs/media only).  
Core implementation remains private during active development.

## Current
- Core repository established and actively developed (private).
- Security baseline in place (`.gitignore`, `.env.example`, secret hygiene).
- CI baseline added (test workflow).
- Overlay stability and UX fixes in progress.

## Next (1-2 sprints)
- Publish `contracts.md` (forecast/event contracts, examples).
- Publish `runtime-flow.md` (end-to-end execution path).
- Publish `reliability.md` (OFF/STUB/LIVE, fallbacks, failure behavior).
- Add concise sprint release notes and demo updates.

## Later
- Sanitized public code snapshot (optional).
- Public core release after security/legal/release gates.
